// src/index.ts

// Export the main client class
export { NebulaClient } from './client';

// Export configuration type and potentially related interfaces/types
export * from './types';

// Export custom error classes for users to catch
export * from './errors';

// We will export Module classes or specific functions later if needed
